"""fireassay — an evaluation integrity layer.

M1: the deterministic spine. Content-addressed suites, append-only runs, a
refusal rule for unsound comparisons, a config matrix, a pure-Python BM25
retriever as the system under test, five deterministic scorers, and a
leaderboard. No LLM call anywhere in this milestone.
"""

__version__ = "0.1.0"
